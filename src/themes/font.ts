import { Khand, Montserrat, Manrope } from "next/font/google";

export const fontKhand = Khand({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
});

export const fontMontserrat = Montserrat({
  subsets: ["latin"],
});

export const fontManrope = Manrope({
  subsets: ["latin"],
});
